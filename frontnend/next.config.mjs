/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "2.bp.blogspot.com",
      "images.unsplash.com",
      "placehold.co",
      "via.placeholder.com",
    ],
  },
};

export default nextConfig;
